package ru.ifmo.rain.usov.generator.grammar_builder.builder_stuff;

public enum GrammarItem {
    NON_TERM, QUOTE, ARROW, END_RULE, END_GRAMMAR, DEF, ATTR, CODE, START,
    QUESTION, WILDCARD, PLUS, START_REGEX, END_REGEX, ALTER,
    LOWER, UPPER, LETTER, DIGIT, NEWLINE, TAB, CARRIAGE
}
